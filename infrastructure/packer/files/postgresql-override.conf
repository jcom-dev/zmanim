# Override to ensure postgresql@.service starts after firstboot configures /data
# This file is placed at /etc/systemd/system/postgresql@17-main.service.d/override.conf

[Unit]
# Wait for firstboot to complete (creates /data/postgres directory)
After=zmanim-firstboot.service
Requires=zmanim-firstboot.service
